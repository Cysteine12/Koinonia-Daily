# Test environment

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/koinonia_daily
spring.datasource.username=postgres
spring.datasource.password=password

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.jpa.open-in-view=false
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT
app.jwt-secret=35b26fdd1507c2d7eea2cce7a8cb7c86b2ab71a5
app.seeder-user-password=password-12345

# AWS mock config
app.aws.access-key=mock-aws-access-key
app.aws.secret-key=mock-aws-secret-key
app.aws.region=mock-aws-region
app.aws.ses-from-email=mock-aws-ses-from-email
app.aws.s3-bucket-name=mock-aws-s3-bucket-name
