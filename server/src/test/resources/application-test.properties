# Test environment

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/koinonia_daily_test
spring.datasource.username=gbenga
spring.datasource.password=password

# JPA / Hibernate\
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.jpa.open-in-view=false
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

app.base-url=http://localhost:8080

# JWT
app.jwt-secret=35b26fdd1507c2d7eea2cce7a8cb7c86b2ab71a5
app.seeder-user-password=password-12345

# AWS mock config
app.aws.access-key=mock-aws-access-key
app.aws.secret-key=mock-aws-secret-key
app.aws.region=mock-aws-region
app.aws.s3-bucket-name=mock-aws-s3-bucket-name

app.email.from=somemail@gmail.com
app.email.provider=mailtrap
app.mailtrap.username=api
app.mailtrap.password=fake-mailtrap-password

app.embedding.queue.provider=local
app.embedding.model.provider=text-embedding-3-small
app.aws.sqs.queue-url=https://{region}.amazonaws.com/{code}/embedding-queue-name

app.openai-api-key=mock-openai-api-key

app.transcription.provider=modal
app.transcription.modal.url=mock-modal-url
app.transcription.modal.api-key=mock-modal-api-key
app.transcription.modal.callback-secret=mock-modal-callback-secret

