info:
  name: resetPassword
  type: http
  seq: 2
  tags:
    - auth-controller

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/reset-password"
  body:
    type: json
    data: |-
      {
        "email": "",
        "password": "",
        "otp": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/auth/reset-password"
      method: POST
      body:
        type: json
        data: |-
          {
            "email": "",
            "password": "",
            "otp": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": true,
            "message": ""
          }
