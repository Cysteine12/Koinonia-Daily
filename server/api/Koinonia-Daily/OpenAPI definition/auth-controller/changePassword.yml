info:
  name: changePassword
  type: http
  seq: 9
  tags:
    - auth-controller

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/change-password"
  body:
    type: json
    data: |-
      {
        "currentPassword": "",
        "newPassword": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/auth/change-password"
      method: POST
      body:
        type: json
        data: |-
          {
            "currentPassword": "",
            "newPassword": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": ""
          }
