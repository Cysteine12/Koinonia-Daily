info:
  name: updateHistoryMarkAsRead
  type: http
  seq: 1
  tags:
    - history-controller

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/histories/:id/marked-read"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "isMarkedRead": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/histories/:id/marked-read"
      method: PATCH
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "isMarkedRead": false
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": true,
            "message": ""
          }
