info:
  name: updateBookmarkNote
  type: http
  seq: 3
  tags:
    - bookmark-controller

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/bookmarks/:id/note"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "note": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/bookmarks/:id/note"
      method: PATCH
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "note": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": ""
          }
