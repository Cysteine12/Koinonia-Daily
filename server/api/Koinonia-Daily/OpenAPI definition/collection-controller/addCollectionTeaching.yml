info:
  name: addCollectionTeaching
  type: http
  seq: 6
  tags:
    - collection-controller

http:
  method: POST
  url: "{{baseUrl}}/api/v1/collections/:id/teachings"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "teachingId": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/collections/:id/teachings"
      method: POST
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "teachingId": 0
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": true,
            "message": ""
          }
