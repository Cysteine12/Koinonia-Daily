info:
  name: recordClick
  type: http
  seq: 1
  tags:
    - search-controller

http:
  method: POST
  url: "{{baseUrl}}/api/v1/search/clicks"
  body:
    type: json
    data: |-
      {
        "teachingId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/search/clicks"
      method: POST
      body:
        type: json
        data: |-
          {
            "teachingId": 0
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": ""
          }
