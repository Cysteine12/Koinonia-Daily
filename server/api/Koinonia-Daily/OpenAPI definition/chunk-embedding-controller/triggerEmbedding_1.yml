info:
  name: triggerEmbedding_1
  type: http
  seq: 1
  tags:
    - chunk-embedding-controller

http:
  method: POST
  url: "{{baseUrl}}/api/v1/chunk-embeddings/trigger"
  body:
    type: json
    data: |-
      {
        "teachingIds": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/chunk-embeddings/trigger"
      method: POST
      body:
        type: json
        data: |-
          {
            "teachingIds": []
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": ""
          }
