info:
  name: triggerTranscription
  type: http
  seq: 6
  tags:
    - transcript-controller

http:
  method: POST
  url: "{{baseUrl}}/api/v1/transcripts/workflow/trigger"
  body:
    type: json
    data: |-
      {
        "transcriptId": 0,
        "audioUrl": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/transcripts/workflow/trigger"
      method: POST
      body:
        type: json
        data: |-
          {
            "transcriptId": 0,
            "audioUrl": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": ""
          }
