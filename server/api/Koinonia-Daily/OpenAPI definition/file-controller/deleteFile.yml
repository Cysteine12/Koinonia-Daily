info:
  name: deleteFile
  type: http
  seq: 2
  tags:
    - file-controller

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/file"
  params:
    - name: objectKey
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/file"
      method: DELETE
      params:
        - name: objectKey
          value: ""
          type: query
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": true,
            "message": ""
          }
