import Login from '@/screens/auth/login';

/**
 * Renders the Login screen.
 *
 * @returns A React element that renders the Login component
 */
export default function LoginScreen() {
  return <Login />;
}
