import VerifyEmail from '@/screens/auth/verify-email';

/**
 * Render the VerifyEmail screen component.
 *
 * @returns The JSX element that renders the `VerifyEmail` screen.
 */
export default function VerifyEmailScreen() {
  return <VerifyEmail />;
}
