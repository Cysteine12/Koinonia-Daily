import Register from '@/screens/auth/register';

/**
 * Render the Register screen component.
 *
 * @returns The JSX element for the Register screen.
 */
export default function RegisterScreen() {
  return <Register />;
}
