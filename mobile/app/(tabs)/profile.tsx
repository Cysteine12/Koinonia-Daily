import Profile from '@/screens/profile/profile';

/**
 * Render the Profile screen.
 *
 * @returns A React element that renders the imported `Profile` component.
 */
export default function ProfileScreen() {
  return <Profile />;
}
