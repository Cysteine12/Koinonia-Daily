import { ThemedView } from '@/components/themed-view';
import { Text } from 'react-native';

export const Login = () => {
  return (
    <ThemedView>
      <Text>Login Screen</Text>
    </ThemedView>
  );
};
